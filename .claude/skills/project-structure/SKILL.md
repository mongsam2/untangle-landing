---
name: project-structure
description: 이 저장소의 Next.js App Router 구조 규약. 파일이나 디렉터리를 만들거나 옮기거나 이름을 정할 때, 새 기능·컴포넌트·라우트·API를 추가할 때, Route Handler(route.ts)와 서버 함수(actions.ts) 중 하나를 고를 때, "use client" 경계를 정할 때 코드를 쓰기 전에 읽는다. app·sections·components·lib·tests 중 어디에 둘지 판단이 필요하면 이 스킬이다. Use when creating, moving, or naming files, adding a feature or endpoint, or choosing a server entry point.
---

`docs/conventions/architecture.md`를 읽고 그대로 따른다.

이 파일은 규칙 본문을 담지 않는다. 규약이 바뀌면 그 문서만 고친다.
